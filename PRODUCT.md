# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 주 사용자는 한 사람, ECLAB(순천향대) 연구실의 본인. 연구실 Windows PC 와 맥미니 데스크톱 브라우저를 오가며 작업한다.
- 보조 기기는 아이폰. Safari 홈 화면 PWA 로 꺼내 보고, 공유 시트 → 단축어로 넣는다.
- 상황: 한 기기에서 복사한 글·링크·파일을 다른 기기에서 바로 써야 할 때. 메신저 "나에게 보내기" 대신 쓴다.
- 나중에 교수님·연구부원에게 소개할 수도 있지만, v1 은 개인 실사용 우선. 발표·데모용 아님.

## Product Purpose

- 어디서 열어도 같은 내용물이 보이는 나만의 상자. 한쪽에서 넣으면 다른 쪽에 1초 안에 뜬다.
- 성공 기준: 2주 동안 매일 쓰고도 불편하지 않다. 넣기(Ctrl+V, 드래그, 입력)와 꺼내기(복사, 다운로드)가 손에 붙는다.

## Positioning

- 데이터는 내 Supabase 프로젝트에만 있고 RLS 로 본인만 접근한다. 운영 비용 0원, 관리할 서버 0대.
- 메신저 대체재이지만 대화가 아니라 "물건을 넣어 두는 상자" 다. 항목마다 출처 기기와 만료가 붙는다.

## Operating Context

- 데스크톱: 입력창, 화면 어디서든 Ctrl+V(텍스트·이미지), 파일 드래그&드롭, 파일 선택, 검색 단축키 `/`.
- 아이폰: 단축어가 Edge Function `share` 로 POST. 개인 토큰(`ec_…`)은 설정에서 발급·폐기.
- 항목 종류: 텍스트, 링크(단일 URL 자동 감지), 파일(≤50MB, 실행 파일 차단). 이미지는 썸네일.
- 만료: 1시간·1일·7일·영구, 기본 7일. 만료 정리는 앱 로드 시 클라이언트가 수행.
- 실시간: Supabase Realtime. 연결 상태(실시간·연결 중·오프라인)가 화면에 보인다.
- 설치: PC/맥은 브라우저 "설치", 아이폰은 홈 화면에 추가. 다크·라이트는 시스템 설정을 따른다.
- 연구실 조명 아래 데스크톱 모니터가 주 장면. 밤에 아이폰으로 보는 경우가 보조.

## Capabilities and Constraints

- 기능(v1 확정): 넣기(텍스트·링크·파일), 실시간 목록, 복사, 열기, 다운로드, 다른 앱으로 공유(Web Share), 고정, 검색, 종류 필터(전체·텍스트·링크·이미지·파일), 만료, 기기 이름, 토큰 관리, 로그아웃.
- 인증: 이메일+비밀번호 기본, 메일 링크 보조.
- 제외: Android 공유 대상, 성능 측정 로그, 분석 도구, 협업·공유 링크. Android 기기 없음.
- UI 언어는 한국어. 말투는 "~해요" 체(기존 카피 유지). 기술 용어(Ctrl+V, Enter, PWA)는 그대로 쓴다.
- 스택: Vite + React 18 + TypeScript strict, CSS 변수. 상태 관리 라이브러리 없음. 외부 UI 라이브러리 없음.
- 검색·필터는 클라이언트에서 최근 500개를 대상으로.

## Brand Commitments

- 이름 "Ender Chest": 마인크래프트의 엔더 상자(어느 상자를 열어도 같은 내용물)에서 따옴. 이 비유가 제품 설명의 전부다.
- 아이콘은 상자 형태를 유지한다(현재 `public/icon.svg`, `scripts/make-icons.mjs` 로 PNG 생성).
- 브랜드 자산(로고 원본, 색 지정)은 따로 없다. 시각 결정은 DESIGN.md 가 맡는다.

## Evidence on Hand

- 기획서 `docs/PLAN.md`, 구현 스펙 `docs/superpowers/specs/2026-09-25-ender-chest-v1-design.md`, 진행 상황 `docs/PROGRESS.md`.
- 실사용 데이터·후기·스크린샷 없음. 마케팅 문구·고객 사례를 만들어 넣지 않는다.

## Product Principles

- 도구는 작업 뒤로 사라진다. 장식보다 상태(연결, 만료, 고정)가 먼저 보인다.
- 데스크톱 넣기 경로(Ctrl+V, 드래그, 키보드)가 최우선. 아이폰은 보조.
- 한 화면, 한 목록. 항목 종류가 섞여도 훑어보기 쉬워야 한다.
- 개인 도구지만 대충 만들지 않는다. 상태 전부(hover, focus, 비어 있음, 로딩, 오류)를 갖춘다.
